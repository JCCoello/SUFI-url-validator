# SUFI-40 — Manual Smoke (run log)

- **Fecha:** 2026-05-29
- **Tester:** JCCoello (juancarlos.coello@applydigital.com)
- **Ticket:** [SUFI-40](https://reigncl.atlassian.net/browse/SUFI-40) — `[at-icon]` Soporte de íconos custom SVG (x, tiktok, numéricos y variantes circle-fill)
- **Entorno:**
  - Storybook: https://sufi-acl.vercel.app/?path=/docs/foundations-iconography--documentation
  - Preview: https://sufi-app.vercel.app/
- **Folder Testmo:** SUFI-40 - Manual Smoke
- **Resultado global:** ✅ Pass (8/8 ejecutados)

---

## Resumen por caso

### 1. [AC1] Íconos custom (x y tiktok) renderizan elemento SVG en lugar de icon-font
**Prioridad:** Critical · **Resultado:** ✅ Pass

- **GIVEN** el `at-icon` en Storybook con el ícono `x` seleccionado en el control
- **WHEN** inspeccionar el DOM del componente con DevTools
- **THEN** verificar que el elemento renderizado es un SVG inline y que no se usa una clase de icon-font
- **Esperado:** el componente renderiza un elemento SVG para el ícono `x` y el mismo comportamiento se confirma para `tiktok` sin trazas del renderizado por icon-font
- **Observado (2026-05-29):** verificado en Storybook con DevTools — `x` y `tiktok` renderizan SVG inline sin trazas de icon-font

---

### 2. [AC2] Íconos estándar de la librería siguen usando el icon-font como fallback
**Prioridad:** Critical · **Resultado:** ✅ Pass

- **GIVEN** el `at-icon` en Storybook con un ícono estándar (no incluido en la lista custom) seleccionado en el control
- **WHEN** inspeccionar el DOM del componente con DevTools
- **THEN** verificar que el elemento usa la clase de icon-font correspondiente y no un SVG inline
- **Esperado:** el ícono estándar se renderiza correctamente mediante el icon-font sin errores visuales ni regresiones respecto al comportamiento previo
- **Observado (2026-05-29):** verificado en Storybook con DevTools — los íconos estándar siguen usando el icon-font sin regresiones ni SVG inline inesperado

---

### 3. [AC3] La prop size se respeta en íconos custom en diferentes tamaños
**Prioridad:** High · **Resultado:** ✅ Pass

- **GIVEN** el `at-icon` en Storybook con el ícono `number-one` seleccionado
- **WHEN** cambiar el control de `size` por cada valor disponible (small, medium, large, xlarge, 3xlarge) y observar el componente
- **THEN** confirmar que el SVG cambia de dimensiones visualmente en cada valor de size
- **Esperado:** el ícono `number-one` se renderiza en las dimensiones correctas para cada valor de `size` y el mismo comportamiento se verifica para `x` y `tiktok`
- **Observado (2026-05-29):** verificado en Storybook — el SVG cambia de dimensiones en cada valor de `size` para `number-one`, `x` y `tiktok` sin quedar fijo en un tamaño

---

### 4. [AC4] La prop color se respeta en íconos SVG custom
**Prioridad:** High · **Resultado:** ✅ Pass

- **GIVEN** el `at-icon` en Storybook con el ícono `x` y un color personalizado definido en el control de color
- **WHEN** inspeccionar el SVG renderizado con DevTools
- **THEN** verificar que el `fill` o el `currentColor` del SVG refleja el valor de la prop `color` definida
- **Esperado:** el SVG del ícono custom aplica el color indicado por la prop y el cambio de color en el control se refleja inmediatamente en el renderizado
- **Observado (2026-05-29):** verificado en Storybook con DevTools — el SVG aplica `currentColor` correctamente y el cambio de color en el control se refleja de inmediato en el renderizado

---

### 5. [AC5] Las props aria-* y className se aplican al SVG de íconos custom
**Prioridad:** High · **Resultado:** ✅ Pass

- **GIVEN** el `at-icon` en Storybook con el ícono `x` y los controles de `aria-label` y `className` configurados con valores de prueba
- **WHEN** inspeccionar el elemento SVG renderizado con DevTools
- **THEN** verificar que el SVG incluye el atributo `aria-label` con el valor esperado y la `className` personalizada en su lista de clases
- **Esperado:** el SVG del ícono custom expone los atributos `aria-*` y la `className` indicados sin que ninguno sea ignorado o descartado
- **Observado (2026-05-29):** verificado en Storybook con DevTools — el SVG incluye `aria-label` y `className` con los valores configurados sin que ninguno sea descartado

---

### 6. [AC6] Los 10 íconos numéricos (number-zero a number-nine) renderizan el dígito correcto
**Prioridad:** High · **Resultado:** ✅ Pass

- **GIVEN** el `at-icon` en Storybook
- **WHEN** seleccionar cada ícono numérico por turno (number-zero a number-nine) en el control de ícono
- **THEN** inspeccionar que cada ícono muestra el dígito esperado (0–9) sin errores visuales ni confusión entre íconos adyacentes
- **Esperado:** cada uno de los 10 íconos numéricos renderiza el dígito correcto sin errores ni SVG en blanco o roto
- **Observado (2026-05-29):** verificado en Storybook — los 10 íconos numéricos (number-zero a number-nine) renderizan el dígito correcto sin confusión entre íconos adyacentes ni SVG roto

---

### 7. [AC7] Variantes circle-fill-tiktok y circle-fill-x se distinguen visualmente de tiktok y x
**Prioridad:** Medium · **Resultado:** ✅ Pass

- **GIVEN** el `at-icon` en Storybook
- **WHEN** comparar el renderizado del ícono `tiktok` con `circle-fill-tiktok` y luego el ícono `x` con `circle-fill-x`
- **THEN** inspeccionar que la variante `circle-fill` incluye un fondo circular visible alrededor del símbolo mientras que la variante simple no lo tiene
- **Esperado:** las variantes `circle-fill` muestran el ícono dentro de un contenedor circular y las variantes simples muestran solo el símbolo sin fondo adicional
- **Observado (2026-05-29):** verificado en Storybook — `circle-fill-tiktok` y `circle-fill-x` muestran el fondo circular correctamente; diferencia visual clara respecto a las variantes simples

---

### 8. [AC8] Footer del sitio muestra los íconos de X y TikTok correctamente
**Prioridad:** Critical · **Resultado:** ✅ Pass

- **GIVEN** el Footer en https://sufi-app.vercel.app/
- **WHEN** desplazarse hasta el footer de la página e inspeccionar los íconos de redes sociales con DevTools
- **THEN** verificar que los íconos de X (Twitter) y TikTok son elementos SVG y que no aparecen caracteres de sustitución ni nombres de clase como texto visible
- **Esperado:** los íconos de X y TikTok en el footer se renderizan correctamente como SVG con la apariencia visual esperada y sin artefactos de fallback
- **Observado (2026-05-29):** verificado en preview — los íconos de X y TikTok en el footer son SVG correctos sin caracteres de sustitución ni artefactos de fallback

---

## Notas

- Sin defectos encontrados durante esta sesión.
- Evidencia visual (Storybook, preview, DevTools) se adjunta directamente al ticket.
