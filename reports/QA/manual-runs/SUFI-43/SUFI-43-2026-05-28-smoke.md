# SUFI-43 — Manual Smoke (run log)

- **Fecha:** 2026-05-28
- **Tester:** JCCoello (juancarlos.coello@applydigital.com)
- **Ticket:** [SUFI-43](https://reigncl.atlassian.net/browse/SUFI-43) — `[ml-tag]` Actualizar colores del `ml-tag` a la nueva paleta (gray, green, violet, yellow)
- **Entorno:**
  - Storybook: https://sufi-acl.vercel.app/?path=/story/molecules-tag--default
  - Preview (test-dev): https://sufi-app.vercel.app/test-dev
  - Figma: 🔴 DS SuFi — v1.0 (node 16-64)
- **Folder Testmo:** SUFI-43 - Manual Smoke
- **Resultado global:** ✅ Pass (8/8 ejecutados)

---

## Resumen por caso

### 1. Variantes gray, green, violet y yellow disponibles en los controls del ml-tag
**Prioridad:** Medium · **Resultado:** ✅ Pass

- **GIVEN** el `ml-tag` en Storybook (`/story/molecules-tag--default`)
- **WHEN** abrir el control de variante de color del componente
- **THEN** inspeccionar las opciones disponibles en el selector de variante
- **Esperado:** las opciones gray, green, violet y yellow aparecen en el control y no falta ninguna de las 4 variantes activas definidas en la propuesta de Sufi
- **Observado (2026-05-28):** verificado en Storybook — las 4 variantes (gray, green, violet, yellow) aparecen como opciones en el selector de variante sin que falte ninguna

---

### 2. Colores de la paleta anterior no aparecen o el breaking change está documentado
**Prioridad:** High · **Resultado:** ✅ Pass

- **GIVEN** el listado previo de colores del `ml-tag` y los controls del componente en Storybook (`/story/molecules-tag--default`)
- **WHEN** revisar las opciones disponibles en el control de variante de color
- **THEN** confirmar que los colores de la paleta anterior no aparecen como opciones o que existe documentación del breaking change en el CHANGELOG o en la descripción del componente
- **Esperado:** los colores de la paleta anterior no están presentes como opciones seleccionables o el breaking change correspondiente está documentado
- **Observado (2026-05-28):** verificado en Storybook — los colores de la paleta anterior no aparecen como opciones seleccionables en el control de variante

---

### 3. Cada variante activa aplica el color de fondo y texto esperado
**Prioridad:** Critical · **Resultado:** ✅ Pass

- **GIVEN** el `ml-tag` en Storybook con cada variante seleccionada por turno (gray, green, violet, yellow)
- **WHEN** inspeccionar los estilos del componente en cada variante con DevTools
- **THEN** comparar el color de fondo y el color de texto contra los tokens de la nueva paleta definidos en Figma (node 16-64)
- **Esperado:** cada variante aplica el color de fondo y de texto correcto definido en la nueva paleta sin valores residuales de la paleta anterior
- **Observado (2026-05-28):** verificado en Storybook con DevTools — cada variante (gray, green, violet, yellow) aplica el color de fondo y de texto correcto de la nueva paleta sin valores residuales de la paleta anterior

---

### 4. Contraste texto y fondo cumple WCAG AA en todas las variantes activas
**Prioridad:** Critical · **Resultado:** ✅ Pass

- **GIVEN** el `ml-tag` en Storybook con cada variante activa seleccionada por turno (gray, green, violet, yellow)
- **WHEN** medir el contraste entre el color del texto y el color de fondo con una herramienta de accesibilidad
- **THEN** inspeccionar el ratio de contraste reportado para cada variante
- **Esperado:** el ratio de contraste es al menos 4.5:1 para texto normal en las cuatro variantes cumpliendo WCAG AA
- **Observado (2026-05-28):** verificado en Storybook — el ratio de contraste es ≥ 4.5:1 en las cuatro variantes activas cumpliendo WCAG AA

---

### 5. Estado deshabilitado usa lógica de esquema de color sin opacidad CSS
**Prioridad:** High · **Resultado:** ✅ Pass

- **GIVEN** el `ml-tag` en Storybook con la opción `disabled` activada en los controls
- **WHEN** inspeccionar los estilos computados del componente deshabilitado con DevTools
- **THEN** verificar que no existe una regla `opacity` reducida aplicada al estado `disabled` y que los colores de fondo y texto corresponden a los valores del esquema de color para ese estado
- **Esperado:** el estado `disabled` no aplica `opacity` CSS sino que utiliza los colores del esquema de color definido para el estado deshabilitado
- **Observado (2026-05-28):** verificado en Storybook con DevTools — el estado `disabled` no aplica `opacity` CSS; los colores de fondo y texto corresponden a los valores del esquema de color para ese estado

---

### 6. Apariencia del estado deshabilitado coincide con Figma en cada variante
**Prioridad:** High · **Resultado:** ✅ Pass

- **GIVEN** el `ml-tag` en Storybook con `disabled` activado y cada variante (gray, green, violet, yellow) seleccionada por turno
- **WHEN** comparar la apariencia visual de cada variante deshabilitada contra la especificación en Figma (node 16-64)
- **THEN** verificar que el color de fondo y texto en estado `disabled` coincide con la especificación de diseño para cada variante
- **Esperado:** cada variante deshabilitada muestra los colores correctos del esquema según el diseño sin diferencias visuales inesperadas
- **Observado (2026-05-28):** verificado en Storybook comparando contra Figma (node 16-64) — cada variante deshabilitada (gray, green, violet, yellow) muestra los colores correctos del esquema de color sin diferencias visuales inesperadas

---

### 7. Storybook documenta la nueva paleta del ml-tag con ejemplos visuales
**Prioridad:** Medium · **Resultado:** ✅ Pass

- **GIVEN** la documentación del `ml-tag` en Storybook (`/story/molecules-tag--default`) y sus historias relacionadas
- **WHEN** navegar la sección de documentación y los controls del componente
- **THEN** inspeccionar la presencia de un ejemplo visual por cada variante activa de la nueva paleta
- **Esperado:** la documentación incluye un ejemplo visual por cada variante activa (gray, green, violet, yellow) con su nombre correspondiente
- **Observado (2026-05-28):** verificado en Storybook — la documentación incluye un ejemplo visual por cada variante activa (gray, green, violet, yellow) con su nombre correspondiente

---

### 8. ml-tag en el sitio de pruebas usa los colores de la nueva paleta
**Prioridad:** High · **Resultado:** ✅ Pass

- **GIVEN** el `ml-tag` renderizado en el sitio de pruebas (`/test-dev`)
- **WHEN** inspeccionar las instancias del componente `ml-tag` presentes en la página con DevTools
- **THEN** comparar los colores de fondo y texto de cada instancia contra los tokens de la nueva paleta (gray, green, violet, yellow)
- **Esperado:** cada instancia del `ml-tag` en el sitio de pruebas aplica los colores correctos de la nueva paleta sin colores obsoletos de la paleta anterior
- **Observado (2026-05-28):** verificado en el sitio de pruebas (`/test-dev`) con DevTools — todas las instancias del `ml-tag` aplican los colores correctos de la nueva paleta (gray, green, violet, yellow) sin colores obsoletos de la paleta anterior

---

## Notas

- Evidencia visual (Storybook, DevTools de contraste, preview) se adjunta al ticket de Jira.
- Tokens a verificar contra DS v1.0 Figma node 16-64.
- Si algún caso falla, anotar token observado vs esperado y capturas en el ticket.
