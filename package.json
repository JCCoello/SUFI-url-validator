{
  "name": "contentful-asset-validator",
  "version": "1.0.0",
  "description": "Automated validator for Contentful image assets using Playwright",
  "main": "index.js",
  "scripts": {
    "validate": "ts-node src/validator.ts",
    "validate:release": "ts-node src/validator.ts --release",
    "test": "playwright test",
    "test:contentful": "playwright test tests/assets.allure.spec.ts --project=production",
    "test:batch": "playwright test tests/assets.spec.ts",
    "test:local": "playwright test tests/localAssets.spec.ts",
    "report": "allure generate allure-results --clean -o allure-report && allure open allure-report --port 4040",
    "report:generate": "allure generate allure-results --clean -o allure-report",
    "test:report": "npm run test:contentful && npm run report:generate && allure open allure-report --port 4040",
    "test:report:batch": "npm run test:batch && npm run report",
    "build": "tsc --noEmit",
    "lint": "eslint src tests --ext .ts"
  },
  "dependencies": {
    "csv-parse": "^5.5.6",
    "dotenv": "^16.4.5",
    "fast-csv": "^5.0.1",
    "form-data": "^4.0.5",
    "p-limit": "^3.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/form-data": "^2.2.1",
    "@types/node": "^20.12.12",
    "allure-commandline": "^2.38.1",
    "allure-js-commons": "^3.7.2",
    "allure-playwright": "^3.7.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
